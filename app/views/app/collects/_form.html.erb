<%= form_for collect do |form| %>
  <% if collect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collect.errors.count, "error") %> prohibited this collect from being saved:</h2>

      <ul>
      <% collect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control", required: true %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, class: "form-control", required: true %>
  </div>

  <div class="field">
    <%= form.label :searched_amount %>
    <%= form.number_field :searched_amount, class: "form-control", required: true %>
  </div>


  <div class="text-right mt-3">
    <%= form.submit "Sauvegarder", class: "btn btn-primary text-white" %>
  </div>
<% end %>

<% content_for :page_name, 'view.posts' %>